---
import { Icon } from "astro-icon/components";
import Card from "@components/ui/Card.astro";
import { highlights } from "@data/home";
---

<section class="section-padding bg-white">
  <div class="container-page">
    <div class="mb-10 max-w-2xl">
      <p class="mb-3 text-sm font-semibold uppercase tracking-[0.14em] text-accent">
        Focus Areas
      </p>
      <h2 class="section-heading">What Manoj builds</h2>
    </div>

    <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
      {
        highlights.map((item) => (
          <Card>
            <Icon name={item.icon} class="mb-5 h-8 w-8 text-accent" aria-hidden="true" />
            <p class="text-3xl font-bold leading-none text-primary">{item.value}</p>
            <p class="mt-3 text-sm leading-6 text-text-muted">{item.label}</p>
          </Card>
        ))
      }
    </div>
  </div>
</section>
